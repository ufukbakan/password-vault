import { StyleSheet } from "react-native"

export const globalStyles = StyleSheet.create({
    input: {
        borderWidth: 1,
        borderColor: "#ccc",
        padding: 5,
        marginVertical: 10,
        minWidth: 200,
        paddingLeft: 10,
        fontSize: 16,
        borderRadius: 5,
    }
})